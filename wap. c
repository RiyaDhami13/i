#include<stdio.h>
int main(){
  int n,x[1000],i,g=0;
  printf("Enter any number");
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",x[i]);
    if(g<x[i]);
    {
      g=x[i];
    }
  }
  printf("\nThe greatest number is %d",g);
  return 0;
}
